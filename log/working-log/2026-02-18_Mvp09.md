# 2026-02-18_Mvp09

## 🎯 今日の目的
MVP09として、PERFORM の最小検証（段落PERFORM + PERFORM THRU）を追加する。

## 🛠 実施内容
- `samples/cobol/Mvp09Program.cbl` を新規作成
- `samples/data/mvp09/input1.txt` / `expected1.txt` を追加
- `apps/backend/tests/CobolMvpRuntimeMvp09Tests.cs` を追加
- C#ランタイム側に `Mvp09Program` を追加して expected 完全一致テストを実装
- `dotnet test --filter Mvp09` を実行して回帰確認

## 🔍 結果
- Mvp09テストは 1件合格（失敗0）
- `FLAG=1` で `PERFORM PARA-MARK` が実行され、`MARK=Y` を確認
- `PERFORM PARA-A THRU PARA-C-EXIT` により `SEQ=ABC` を確認

## 💡 学び
段落単位の PERFORM と THRU 範囲実行を分離して可視化すると、
変換後の呼び出し順の誤りを検出しやすい。

## 🧠 思考整理
MVP09で「段落実行の基本」を固定したことで、
次段階（UNTIL/VARYING）のループ検証を独立して扱える状態になった。

## ⏭ 次のアクション
- MVP09の内容をルール定義・Coverageへ反映
- TODO抽出を再実行して差分確認
- MVP09完了タグの付与準備
