# 2026-02-18_Mvp10

## 🎯 今日の目的
MVP10として、PERFORM UNTIL（TEST BEFORE）を最小ケースで検証する。

## 🛠 実施内容
- `samples/cobol/Mvp10Program.cbl` を新規作成
- `samples/data/mvp10/input1.txt` / `expected1.txt` を追加
- `samples/data/mvp10/input2.txt` / `expected2.txt` を追加
- `apps/backend/tests/CobolMvpRuntimeMvp10Tests.cs` を追加
- C#ランタイム側に `Mvp10Program` を追加して expected 完全一致テストを実装
- `dotnet test --filter Mvp10` を実行して回帰確認

## 🔍 結果
- Mvp10テストは 2件合格（失敗0）
- `N=3` で `SUM=6`（1+2+3）を確認
- `N=0` で `SUM=0`（ループ本体未実行）を確認

## 💡 学び
PERFORM UNTIL は test-before のため、初期条件次第で本体未実行になる。
この挙動を最小ケースで固定したことで、将来の変換破綻を検出しやすくなった。

## 🧠 思考整理
MVP10で UNTIL を単独検証したことで、
次段階の VARYING（MVP11）を混線なく評価できる準備が整った。

## ⏭ 次のアクション
- MVP10の内容をルール定義・Coverageへ反映
- TODO抽出の再実行で差分確認
- MVP11（PERFORM VARYING）最小ケースの設計
